target
llvm-project

# Editor build. assets/ide/etamil-ide.js IS committed -- GitHub Pages runs
# Jekyll, not npm, so the bundle has to be in the repo. These are the inputs
# and caches that must not be.
node_modules
ide/node_modules
ide/.vite
# wasm-bindgen output: a build input regenerated by `npm run wasm`. The served
# copy is emitted into assets/ide/ by vite and IS committed.
ide/wasm
